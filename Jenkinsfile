node {
echo("pipeline test");
echo("another pipeline test");
	stage('Init')	{
		echo ('Init stage');
		def z = new org.antonb.shared.shared();
		z.init();
		const.Url = "www.spb.ru";
		echo const.Url;
		echo ('Init stage end');
	}
}
